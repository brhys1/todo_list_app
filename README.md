# todo_list_app

How to run locally

3 steps: 
    - running the messaging endpoint
        - ngrok http 5000
    - running the backend
        - cd backend && uv run main.py
    - running the frontend
        - cd frontend && npm run dev

